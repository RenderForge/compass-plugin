# BTV — works with Compass

This repo is connected to Compass, the project's shared record (mission, roadmap, decisions, ADRs, governance). The working rules reach your AI from Compass itself — the MCP server's instructions and the Compass plugin's `compass-workflow` skill — so they are always current. Do not copy them here.

First call of every session: `get_context()`.
